{
  "name": "nocobase",
  "version": "1.0.8",
  "scripts": {
    "start": "node dist/index.js",
    "build": "npx @nocobase/cli build"  // Ajuste para usar o CLI do nocobase, se disponível
  },
  "dependencies": {
    "@nocobase/server": "0.9.0"  // Substitua por uma versão específica compatível
  }
}
